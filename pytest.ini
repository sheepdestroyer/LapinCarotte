[pytest]
python_files = test_*.py
python_classes = Test*
python_functions = test_*
# Add paths to source code if not in PYTHONPATH by default
# pythonpath = . src (adjust as needed if your source is in a subfolder like 'src')
# For this project, assuming main files are at root, so '.' should be fine or not needed.

markers =
    slow: marks tests as slow to run
    integration: marks integration tests
    unit: marks unit tests
